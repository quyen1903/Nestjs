import { User } from '@prisma/client';

declare global {
  namespace Express {
    interface User {
      user: User;
      accessToken: string;
      refreshToken: string;
    }

    interface Request {
      user?: User;
    }
  }
}
